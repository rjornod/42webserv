#pragma once
#include <iostream>
#include "GlobalConfig.hpp"

class ConfigParser {
	private:
		int data; // mock data
	public:
		ConfigParser(char *pathToConfig) {};
		~ConfigParser() {};
		void parseFile() { return;};
};